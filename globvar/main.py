import config
import update

print(config.a)
print(config.b)

#we have created three files: config.py, update.py, and main.py
 #in the main.py file, we import both config.py and update.py module
 # we print and test the values of global variables whether they are changed or not.
